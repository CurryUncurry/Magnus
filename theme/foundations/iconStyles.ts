import { IconProps } from '@chakra-ui/react'

const iconStyles = {
	defaultIcon: {
		w: '20px',
		h: '20px',
		transition: '.3s ease'
	} as IconProps,
	dropdownIcon: {
		w: '8px',
		h: '6px'
	} as IconProps
}

export default iconStyles
